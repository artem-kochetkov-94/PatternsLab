export function Explanation() {
  return (
    <div className="space-y-4 text-slate-300">
      <p>
        Когда для одного ответа клиенту нужно опросить несколько сервисов, есть несколько
        способов это организовать — они по-разному распределяют, кто кого вызывает и кто несёт
        ответственность за итоговый результат.
      </p>

      <div>
        <h3 className="font-semibold text-white">Агрегатор</h3>
        <p className="mt-2">
          Один сервис параллельно опрашивает несколько других и сам склеивает единый ответ.
          Клиент не знает, что за одним запросом стояло несколько внутренних вызовов. Ключевое —
          запросы идут ПАРАЛЛЕЛЬНО, иначе выгоды от агрегации почти нет.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Цепочка</h3>
        <p className="mt-2">
          Сервисы вызывают друг друга последовательно, каждый решает свою часть задачи и передаёт
          эстафету дальше. Просто и предсказуемо, но синхронно: если одно звено медленное или
          упало, вся цепочка ждёт или падает вместе с ним.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Событийно-ориентированная архитектура</h3>
        <p className="mt-2">
          Сервисы не вызывают друг друга напрямую, а публикуют события в общую шину/очередь.
          Общая идея — обработать запрос, отпустить клиента и уже ПОСЛЕ ЭТОГО разобраться с
          событиями. Три разновидности отличаются тем, что именно летит в событии и кто
          дирижирует процессом:
        </p>
        <ul className="mt-2 list-disc space-y-1 pl-5">
          <li>
            <strong className="text-white">Event Notification</strong> — событие несёт только
            факт («что-то произошло»), без данных. Получателю, которому нужны детали, приходится
            идти за ними к источнику отдельным вызовом.
          </li>
          <li>
            <strong className="text-white">State Transfer</strong> — событие несёт с собой
            данные. Прямых вызовов между сервисами больше не нужно — все читают то, что им нужно,
            прямо из потока событий.
          </li>
          <li>
            <strong className="text-white">Event Collaboration</strong> — никто не дирижирует
            централизованно: каждый сервис сам решает, на какие события подписаться и что
            опубликовать в ответ. Итоговый процесс складывается из цепочки независимых реакций,
            а не из явного сценария в одном месте.
          </li>
        </ul>
      </div>

      <div>
        <h3 className="font-semibold text-white">Отложенное выполнение задач</h3>
        <p className="mt-2">
          Частный, но очень частый случай: долгую операцию (обработка видео, генерация отчёта)
          не обязательно делать в рамках того же HTTP-запроса. Задача кладётся в очередь, клиент
          получает ответ почти мгновенно, а сама обработка идёт в фоне — воркер заберёт задачу,
          когда освободится, и переживёт свой собственный рестарт, не потеряв работу.
        </p>
      </div>
    </div>
  );
}
