export function Explanation() {
  return (
    <div className="space-y-4 text-slate-300">
      <p>
        <strong className="text-white">Индекс</strong> — дополнительная
        структура данных, которая ускоряет чтение ценой более медленной
        записи (индекс тоже нужно обновлять при каждом INSERT/UPDATE) и
        лишней памяти на диске. Индекс — не универсальное «сделать быстрее»,
        а конкретная структура под конкретный характер запроса.
      </p>

      <div>
        <h3 className="font-semibold text-white">Пять типов из демо</h3>
        <ul className="mt-2 list-disc space-y-1 pl-5">
          <li>
            <strong>BTree</strong> — сбалансированное дерево, хранит ключи
            упорядоченными. Единственный из пяти, кто умеет диапазоны
            («от … до») и сортировку, а не только точное совпадение.
          </li>
          <li>
            <strong>Hash</strong> — хэш ключа указывает прямо на запись.
            Только точное совпадение, зато быстрее BTree там, где диапазоны
            не нужны.
          </li>
          <li>
            <strong>Bitmap</strong> — по биту на каждое возможное значение
            колонки. Выгоден при низкой кардинальности (мало уникальных
            значений на много строк) — пол, статус заказа, булевы флаги.
          </li>
          <li>
            <strong>Spatial</strong> — структура вроде R-tree/quad-tree под
            двумерные (и более) координаты — запросы «что рядом» и
            «что внутри области».
          </li>
          <li>
            <strong>Reversed (инвертированный)</strong> — слово → список
            документов, где оно встречается. Основа полнотекстового поиска
            (Elasticsearch и внутри самих БД).
          </li>
        </ul>
      </div>

      <div>
        <h3 className="font-semibold text-white">
          Кластерные и некластерные
        </h3>
        <p className="mt-2">
          При наличии <strong>кластерного</strong> индекса строки таблицы
          физически хранятся в том же порядке, что и значения его ключа —
          поэтому кластерный индекс может быть только один. Если его нет,
          таблица — просто неупорядоченная «куча», а любой некластерный
          индекс на ней хранит лишь указатели на нужные строки, а не сами
          данные.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Покрывающий индекс</h3>
        <p className="mt-2">
          Некластерный индекс называют <strong>покрывающим</strong>, если он
          сам содержит все колонки, нужные запросу, — тогда ответ отдаётся
          прямо из индекса, без обращения к самой таблице.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Разряженный индекс</h3>
        <p className="mt-2">
          <strong>Разряженный (sparse) индекс</strong> хранит указатель не
          на каждую запись, а на блок отсортированного файла данных — компромисс
          между размером индекса и скоростью: до нужной записи придётся
          немного досканировать блок, зато сам индекс кратно меньше.
        </p>
      </div>
    </div>
  );
}
