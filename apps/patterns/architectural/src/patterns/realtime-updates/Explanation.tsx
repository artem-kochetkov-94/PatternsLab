export function Explanation() {
  return (
    <div className="space-y-4 text-slate-300">
      <p>
        HTTP по умолчанию — модель «запрос-ответ», где инициатор всегда клиент. Если клиенту
        нужно узнавать об изменениях на сервере, не дожидаясь, пока сам об этом спросит, есть три
        стандартных обходных пути.
      </p>

      <div>
        <h3 className="font-semibold text-white">Polling</h3>
        <p className="mt-2">
          Клиент сам спрашивает «есть что-то новое?» каждые N секунд. Самый простой вариант — но
          два минуса сразу: большинство запросов впустую (ответ «нет»), а данные, появившиеся
          сразу после опроса, клиент увидит только через целый интервал.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Long Polling</h3>
        <p className="mt-2">
          Клиент спрашивает один раз, но сервер не отвечает сразу — держит соединение открытым,
          пока не появятся данные (или не истечёт таймаут). Как только событие произошло, ответ
          уходит немедленно, а клиент сразу открывает новый долгий запрос. Задержка минимальна,
          но сервер вынужден держать много одновременно открытых соединений.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Streaming</h3>
        <p className="mt-2">
          Одно долгоживущее соединение на весь сеанс — сервер сам решает, когда и что прислать, не
          дожидаясь новых запросов вообще (Server-Sent Events, WebSocket). Самый отзывчивый и
          самый эффективный по трафику вариант, но и самый требовательный к инфраструктуре —
          нужно уметь держать много долгих соединений и корректно переживать разрывы связи.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Итого</h3>
        <p className="mt-2">
          Polling — просто, но с задержкой и лишним трафиком. Long Polling — компромисс: меньше
          задержки, чуть больше сложности на сервере. Streaming — минимальная задержка и трафик
          ценой самой высокой инфраструктурной сложности.
        </p>
      </div>
    </div>
  );
}
