export function Explanation() {
  return (
    <div className="space-y-4 text-slate-300">
      <p>
        <strong className="text-white">Партиционирование</strong> — метод разделения больших
        таблиц на много маленьких секций, желательно прозрачно для приложения. Бывает{" "}
        <strong className="text-white">вертикальным</strong> (режем по столбцам — например,
        редко используемые большие поля вроде фото выносим в отдельную секцию) и{" "}
        <strong className="text-white">горизонтальным</strong> (режем по строкам — например, по
        диапазону id). Пока все секции лежат на одном инстансе БД — это просто партиционирование.
      </p>

      <div>
        <h3 className="font-semibold text-white">Шардирование</h3>
        <p className="mt-2">
          Как только каждая секция переезжает на СВОЙ ОТДЕЛЬНЫЙ инстанс базы данных — это уже
          шардирование: горизонтальное партиционирование, разнесённое по независимым машинам.
          Каждый шард обычно держит и собственную реплику — шардирование и репликация работают
          вместе, а не вместо друг друга.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Routing: как понять, куда идти</h3>
        <ul className="mt-2 list-disc space-y-2 pl-5">
          <li>
            <strong className="text-white">Клиентский</strong> — логика маршрутизации живёт в
            самом клиенте. Плюс — нет лишнего сетевого узла; минусы — код дублируется в каждом
            клиенте, и обновление списка хостов усложняется.
          </li>
          <li>
            <strong className="text-white">Proxy</strong> — между клиентом и шардами появляется
            прозрачный узел, который сам решает, куда шёл запрос. Плюс — приложение не знает о
            шардинге вообще; минусы — лишний сетевой хоп, потеря части функциональности БД
            (например, транзакций поперёк шардов), единая точка отказа.
          </li>
          <li>
            <strong className="text-white">Coordinator</strong> — как Proxy, но ещё и умеет
            кэшировать. Плюсы те же плюс кэш; минусы — та же инфраструктурная сложность плюс
            дополнительная нагрузка на сам координатор.
          </li>
        </ul>
      </div>

      <div>
        <h3 className="font-semibold text-white">Rendezvous Hashing</h3>
        <p className="mt-2">
          Альтернатива кольцу: для ключа считаем F(key, shard) для КАЖДОГО шарда и выбираем шард
          с максимальным значением. При выпадении шарда пересчитывать нужно только его ключи —
          свойство минимальной переброски сохраняется, но без кольца и виртуальных узлов.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Перебалансировка</h3>
        <p className="mt-2">
          Перенос данных с одного шарда на другой без остановки системы — отдельная задача.
          Подходы: временно уйти в read-only; сделать данные неизменяемыми (пишем только в
          новый шард (tgt), читаем из старого (src) и нового сразу); настроить логическую
          репликацию src → tgt и переключиться на tgt, когда она догонит; либо смешанный подход
          из перечисленного.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Resharding</h3>
        <p className="mt-2">
          Отдельно от рутинной перебалансировки стоит решардинг — изменение самой СТРАТЕГИИ
          шардирования: добавление/удаление шардов или исправление ошибок в изначальном выборе
          способа шардирования. Именно здесь разница между обычным hash % N и consistent hashing
          (демо рядом) становится критичной: первый при любом решардинге перемешивает почти все
          ключи, второй — только соседей изменённого узла.
        </p>
      </div>
    </div>
  );
}
