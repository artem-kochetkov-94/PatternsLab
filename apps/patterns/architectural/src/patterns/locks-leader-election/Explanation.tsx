export function Explanation() {
  return (
    <div className="space-y-4 text-slate-300">
      <p>
        Два разных вопроса консенсуса: как гарантировать, что ресурс в моменте занят только одним
        клиентом (блокировки), и как узлам самим договориться, кто теперь главный (выбор лидера).
      </p>

      <div>
        <h3 className="font-semibold text-white">Распределённые блокировки</h3>
        <p className="mt-2">
          Обычный <code className="rounded bg-slate-800 px-1">SET key value</code> ничего не
          проверяет — если два клиента напишут по очереди, второй молча перезапишет первого, и
          оба будут уверены, что владеют блокировкой. Правильный вариант —{" "}
          <code className="rounded bg-slate-800 px-1">SET key value NX PX 30000</code>:{" "}
          <strong className="text-white">NX</strong> устанавливает ключ, только если его ещё нет
          (атомарно), <strong className="text-white">PX</strong> — авто-истечение на случай, если
          владелец блокировки упадёт и не освободит её сам.
        </p>
        <p className="mt-2">
          Снимать блокировку тоже нужно осторожно: если просто удалить ключ, можно случайно
          удалить уже НОВУЮ (чужую) блокировку — если наша к этому моменту истекла по PX. Поэтому
          удаление делают Lua-скриптом, который сначала сверяет, что значение всё ещё «моё».
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Выбор лидера (Bully algorithm)</h3>
        <p className="mt-2">
          Когда лидер пропадает, узлам нужно самим выбрать нового — без внешнего арбитра. Bully
          algorithm («алгоритм забияки») — один из самых простых способов: узел, заметивший
          пропажу лидера, шлёт Election всем узлам с БОЛЬШИМ id. Каждый, кто откликнулся, сам
          инициирует выборы среди тех, кто старше уже его. Если узел не получил ни одного ответа
          — значит, он самый старший из живых, и объявляет себя лидером всем остальным.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Raft и Paxos</h3>
        <p className="mt-2">
          Более серьёзные протоколы консенсуса (используются внутри etcd, ZooKeeper, множества
          распределённых БД) решают ту же задачу — выбор лидера и согласование состояния между
          узлами — но с формальными гарантиями корректности даже при частичных сбоях сети,
          задержках и одновременных попытках выборов. Raft спроектирован явно понятным для
          реализации; Paxos старше и считается более сложным для восприятия, хотя математически
          эквивалентен по гарантиям.
        </p>
      </div>
    </div>
  );
}
