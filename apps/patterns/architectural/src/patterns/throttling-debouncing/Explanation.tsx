export function Explanation() {
  return (
    <div className="space-y-4 text-slate-300">
      <p>
        Оба приёма решают одну проблему — событий (клики, ввод в поле поиска, скролл, resize)
        сыплется больше, чем имеет смысл на них реагировать: не нужно слать запрос на сервер при
        каждом нажатии клавиши.
      </p>

      <div>
        <h3 className="font-semibold text-white">Throttle</h3>
        <p className="mt-2">
          Реагировать не чаще, чем раз в N миллисекунд, независимо от того, сколько событий
          пришло за это время. Подходит, когда нужна регулярная, предсказуемая частота реакции —
          например, обновление позиции при скролле или resize: и в начале, и в разгар потока
          событий отклик идёт стабильно, просто не на каждое событие.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Debounce</h3>
        <p className="mt-2">
          Реагировать только после того, как события ПЕРЕСТАЛИ приходить хотя бы на N
          миллисекунд. Идеально для «пользователь закончил печатать» — поиск с автодополнением не
          должен слать запрос на каждую нажатую букву, только когда ввод затих.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Разница в демо</h3>
        <p className="mt-2">
          При серии быстрых кликов throttle сработает несколько раз равномерно по ходу серии — по
          разу на каждый интервал. Debounce не сработает НИ РАЗУ, пока клики идут часто, и
          сработает только один раз — после того, как клики прекратились.
        </p>
      </div>
    </div>
  );
}
