export function Explanation() {
  return (
    <div className="space-y-4 text-slate-300">
      <p>
        Автодополнение — поиск по префиксу: пользователь ввёл несколько символов, нужно за
        миллисекунды вернуть список слов, начинающихся с них, отсортированный по популярности.
        Перебирать все слова на каждый ввод символа — слишком медленно при большом словаре.
      </p>

      <div>
        <h3 className="font-semibold text-white">Trie (префиксное дерево)</h3>
        <p className="mt-2">
          Структура, где путь от корня до узла — это префикс. Все слова с общим началом делят
          общий путь в дереве, поэтому поиск по префиксу — это просто спуск по дереву на длину
          префикса, а не сравнение с каждым словом словаря по отдельности.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Сжатие (radix tree)</h3>
        <p className="mt-2">
          Наивный trie хранит один символ на узел — для длинных слов с уникальными хвостами это
          расточительно по памяти. Сжатая версия схлопывает цепочки узлов с единственным потомком
          в одно ребро с несколькими символами (как «ривет» на ребре, а не р-и-в-е-т по узлу на
          букву) — в демо рядом используется именно она.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Как индекс обновляется</h3>
        <p className="mt-2">
          Пул автодополнения — это отдельный, оптимизированный только под чтение сервис. Он не
          обновляется в реальном времени на каждый чих: запросы пользователей логируются через
          очередь в Analytics/OLAP, а сам индекс (trie с частотами) пересобирается из OLAP
          пакетно — например, раз в день. Пользователь ищет по вчерашней статистике популярности,
          зато сам поиск — за микросекунды и без нагрузки на основную базу.
        </p>
      </div>
    </div>
  );
}
