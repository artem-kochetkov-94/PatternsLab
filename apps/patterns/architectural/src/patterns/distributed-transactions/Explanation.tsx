export function Explanation() {
  return (
    <div className="space-y-4 text-slate-300">
      <p>
        Одна бизнес-операция иногда должна изменить данные сразу в нескольких независимых местах
        — и либо это должно случиться везде, либо нигде. Три разных решения этой задачи.
      </p>

      <div>
        <h3 className="font-semibold text-white">2PC (Two-Phase Commit)</h3>
        <p className="mt-2">
          Координатор сначала спрашивает «готовы?» у всех участников (фаза Prepare), и только
          если ВСЕ ответили «да», даёт команду зафиксировать (фаза Commit). Если хоть один
          против — Rollback у всех. Строго консистентно, но участники блокируют нужные ресурсы на
          всё время голосования — а если координатор упадёт между Prepare и Commit, участники
          могут зависнуть в неопределённом состоянии (in-doubt transaction) до его восстановления.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Saga</h3>
        <p className="mt-2">
          Вместо одной распределённой транзакции — набор ЛОКАЛЬНЫХ транзакций, каждая из которых
          публикует событие, запускающее следующий шаг. Если шаг падает, отката единой транзакции
          не существует — вместо него запускается КОМПЕНСИРУЮЩАЯ операция, которая отменяет эффект
          уже выполненных шагов (например, отмена ранее созданного заказа). Никто не блокируется
          на время всей саги — цена за это: между шагами система временно находится в
          промежуточном, не до конца согласованном состоянии.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Transaction Outbox</h3>
        <p className="mt-2">
          Решает смежную, но другую проблему: <strong className="text-white">dual write</strong> —
          запись в БД и публикация события в очередь — это два разных ресурса, обычная транзакция
          не может атомарно охватить оба сразу. Решение — писать не напрямую в очередь, а в
          отдельную таблицу Outbox, ВНУТРИ ТОЙ ЖЕ локальной транзакции, что и бизнес-данные.
          Отдельный процесс (Publisher) затем надёжно перекладывает записи из Outbox в очередь —
          at-least-once, с гарантией, что событие не потеряется, даже если Publisher упадёт
          посередине.
        </p>
      </div>
    </div>
  );
}
