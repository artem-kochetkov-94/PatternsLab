export function Explanation() {
  return (
    <div className="space-y-4 text-slate-300">
      <p>
        <strong className="text-white">Observability</strong> — способность
        понять, что происходит внутри системы, по тому, что она "выдаёт
        наружу": логам, метрикам и трейсам. В микросервисах один
        пользовательский запрос проходит через десяток сервисов — и логов
        каждого сервиса по отдельности недостаточно, чтобы понять, что
        вообще происходило с запросом целиком.
      </p>

      <div>
        <h3 className="font-semibold text-white">Трейсинг</h3>
        <p className="mt-2">
          <strong>Трейс</strong> — вся история одного запроса от входа до
          выхода. Состоит из <strong>спанов</strong> — отдельных операций
          (вызов сервиса, запрос к БД, поход в кэш), у каждого есть начало,
          длительность и ссылка на родительский спан — так строится дерево
          вложенности, а не просто плоский список событий.
        </p>
        <p className="mt-2">
          Главная польза не в том, что трейс показывает <em>путь</em>{" "}
          запроса — это умеют и логи с request ID. Трейс показывает{" "}
          <strong>время</strong>: сколько заняла каждая операция
          относительно других, включая параллельные. В демо рядом Auth
          укладывается в 40мс, но общая длительность всё равно 180мс — из
          waterfall сразу видно, что тормозит не Auth, а поход Orders-сервиса
          в базу.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Мониторинг</h3>
        <p className="mt-2">
          Метрики — агрегированные числовые показатели во времени: RPS,
          response time, error rate, загрузка CPU/RAM. Prometheus стал
          негласным стандартом: сервис сам отдаёт метрики на HTTP-эндпоинте
          в текстовом формате, а Prometheus периодически их собирает
          (pull-модель) и хранит как time-series.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Логирование</h3>
        <p className="mt-2">
          С ростом числа инстансов логи каждого по отдельности бесполезны —
          нужна централизация: демон на каждой машине собирает локальные
          логи и стримит их в общее хранилище с полнотекстовым поиском
          (стек ELK: Elasticsearch + Logstash + Kibana, или Graylog).
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">
          Анализ сбоев и профилирование
        </h3>
        <p className="mt-2">
          Отдельные инструменты (Sentry и подобные) ловят необработанные
          исключения прямо с контекстом стека вызовов и окружения — без
          необходимости искать нужную ошибку среди миллионов строк лога.{" "}
          <strong>Непрерывное профилирование</strong> (Pyroscope, Parca) идёт
          ещё глубже: постоянно снимает, на что именно тратит время CPU
          каждая функция в проде, — а не только на тестовом стенде во время
          локального профилирования.
        </p>
      </div>
    </div>
  );
}
