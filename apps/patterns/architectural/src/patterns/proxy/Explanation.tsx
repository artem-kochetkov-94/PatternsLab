export function Explanation() {
  return (
    <div className="space-y-4 text-slate-300">
      <p>
        <strong className="text-white">Proxy</strong> — промежуточный узел,
        через который проходит трафик между двумя сторонами, ничего не зная
        о конечном получателе или отправителе так, как знали бы они сами
        друг про друга напрямую. Задач у него много: кэширование, ограничение
        трафика, обход блокировок, анонимность, сжатие и модификация данных
        на лету. Но ключевое архитектурное решение — <em>на чьей он стороне</em>,
        и от этого зависит, кого именно он скрывает.
      </p>

      <div>
        <h3 className="font-semibold text-white">Forward Proxy</h3>
        <p className="mt-2">
          Стоит на стороне <strong>клиента</strong>. Клиент обращается не к
          целевому серверу напрямую, а к proxy — и уже proxy делает запрос
          от своего имени. Целевой сервер видит proxy, а не настоящего
          клиента. Так работает корпоративный прокси (весь исходящий трафик
          сотрудника идёт через него) или обход блокировок: в 2022 году
          Роскомнадзор заблокировал Instagram в России — доступ напрямую
          закрыт, но через Forward Proxy за пределами блокировки запрос
          всё ещё проходит.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Reverse Proxy</h3>
        <p className="mt-2">
          Стоит на стороне <strong>сервера</strong>. Клиент обращается к
          proxy, думая, что это и есть сервер — а тот уже сам решает, на
          какой из реальных backend-инстансов перенаправить запрос. Клиент
          никогда не видит настоящую топологию: сколько backend-ов, где они
          и что с ними происходит. Именно поэтому Reverse Proxy — типичное
          место, куда вешают ещё и балансировку нагрузки, и кэширование, и
          терминацию TLS: он и так уже единая точка входа.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Как не перепутать</h3>
        <p className="mt-2">
          Forward Proxy <strong>клиент настраивает сам</strong> и явно про
          него знает (прописывает адрес прокси в настройках). Reverse Proxy
          клиент вообще не подозревает — с его точки зрения он просто
          обратился к серверу.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Где встречается на практике</h3>
        <p className="mt-2">
          Reverse Proxy — nginx / Envoy / Traefik перед пулом бэкендов, CDN
          перед origin-сервером. Forward Proxy — корпоративные
          прокси-серверы, VPN-клиенты, анонимайзеры, инструменты обхода
          гео-блокировок.
        </p>
      </div>
    </div>
  );
}
