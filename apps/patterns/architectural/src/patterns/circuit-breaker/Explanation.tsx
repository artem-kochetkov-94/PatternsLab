export function Explanation() {
  return (
    <div className="space-y-4 text-slate-300">
      <p>
        <strong className="text-white">Circuit Breaker (Предохранитель)</strong>{" "}
        — паттерн отказоустойчивости: защищает вызывающего от бесполезных
        обращений к упавшему сервису. Название — прямая метафора
        электрического автомата: если через него постоянно "бьёт током"
        (сервис отказывает), он размыкает цепь, чтобы не жечь проводку
        дальше — то есть не тратить время, потоки и соединения на запросы,
        которые почти наверняка провалятся.
      </p>

      <div>
        <h3 className="font-semibold text-white">Три состояния</h3>
        <ul className="mt-2 list-disc space-y-1 pl-5">
          <li>
            <strong>Closed</strong> — обычный режим: запросы идут к сервису,
            а брейкер считает отказы подряд.
          </li>
          <li>
            <strong>Open</strong> — после N отказов подряд цепь размыкается:
            все запросы отбиваются мгновенно (fast fail), сервис вообще не
            трогаем — даём ему время восстановиться.
          </li>
          <li>
            <strong>Half-Open</strong> — по истечении паузы пропускаем ОДИН
            пробный запрос. Прошёл — закрываем цепь и работаем как обычно.
            Не прошёл — снова открываем и ждём ещё столько же.
          </li>
        </ul>
      </div>

      <div>
        <h3 className="font-semibold text-white">
          Почему не просто ретраить
        </h3>
        <p className="mt-2">
          Без брейкера каждый запрос к упавшему сервису всё равно ждёт
          таймаут, занимает поток/соединение и в итоге падает — при высокой
          нагрузке это может уронить и вызывающий сервис тоже (каскадный
          отказ). Брейкер разрывает эту цепочку: пока сервис лежит, ответ
          "не сработает" отдаётся мгновенно, без похода к нему.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Что дальше в реальных системах</h3>
        <ul className="mt-2 list-disc space-y-1 pl-5">
          <li>
            Порог обычно считают не по подряд идущим отказам, а по проценту
            ошибок за скользящее окно времени — устойчивее к редким сбоям.
          </li>
          <li>
            В half-open часто пускают не один, а несколько пробных запросов,
            прежде чем закрыть цепь полностью.
          </li>
          <li>
            Открытая цепь — хорошее место для fallback: отдать закэшированный
            ответ или деградированную версию функциональности вместо ошибки.
          </li>
        </ul>
      </div>

      <div>
        <h3 className="font-semibold text-white">Где встречается на практике</h3>
        <p className="mt-2">
          Netflix Hystrix (классика, задавшая паттерн), resilience4j и Polly
          в Java/.NET-экосистемах, Istio/Envoy на уровне service mesh —
          настраиваемый circuit breaking без единой строчки кода в сервисе.
        </p>
      </div>
    </div>
  );
}
