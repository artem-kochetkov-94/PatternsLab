export function Explanation() {
  return (
    <div className="space-y-4 text-slate-300">
      <p>
        Три независимых способа не дать одному сбойному или перегруженному участку положить всю
        систему целиком.
      </p>

      <div>
        <h3 className="font-semibold text-white">Retries, Idempotency, Backoff</h3>
        <p className="mt-2">
          Повторить упавший запрос — разумная реакция на временный сбой (таймаут, кратковременная
          перегрузка). Но есть баланс: «пользовательский запрос — расшибись, но ответь» против
          «лучше ответить ошибкой, чем добить и без того перегруженный сервис лавиной ретраев».
        </p>
        <p className="mt-2">
          <strong className="text-white">Идемпотентность</strong> — свойство операции возвращать
          тот же результат при повторном применении. GET, PUT, DELETE обычно идемпотентны —
          повторять безопасно. Голый POST — нет: повторный вызов может создать вторую сущность
          или списать деньги дважды (без отдельной защиты вроде idempotency key).
        </p>
        <p className="mt-2">
          <strong className="text-white">Backoff</strong> — пауза перед повтором, растущая с
          каждой попыткой (экспоненциально: 100мс → 200мс → 400мс…). Не даёт множеству клиентов
          синхронно забомбардировать едва восстановившийся сервис.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Backpressure</h3>
        <p className="mt-2">
          Если Service 1 шлёт 150 запросов в секунду, а Service 2 успевает обработать только 100,
          разница копится в очереди — и без вмешательства растёт бесконечно. Варианты: отбрасывать
          лишнее (drop), держать буфер ограниченного размера и отвечать 429 при переполнении, либо
          настоящий backpressure — сигнал производителю притормозить самому.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Graceful Degradation и Fallback</h3>
        <p className="mt-2">
          <strong className="text-white">Graceful Degradation</strong> — под нагрузкой система
          сама отключает необязательные функции (реклама, необязательные плюшки), чтобы сохранить
          критичный путь (у такси — доехать и оплатить) в рабочем состоянии.
        </p>
        <p className="mt-2">
          <strong className="text-white">Fallback</strong> — если конкретная зависимость
          недоступна, вместо ошибки подставляется заранее заготовленный дефолтный ответ (dummy-
          рекомендации вместо персонализированных). Пользователь получает рабочий, пусть и менее
          качественный результат, вместо явного сбоя.
        </p>
      </div>
    </div>
  );
}
