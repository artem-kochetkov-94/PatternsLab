export function Explanation() {
  return (
    <div className="space-y-4 text-slate-300">
      <p>
        <strong className="text-white">Балансировка нагрузки (Load Balancing)</strong>{" "}
        — это архитектурный паттерн, который распределяет входящие запросы
        между несколькими одинаковыми инстансами сервиса. Без неё
        горизонтальное масштабирование не имеет смысла: можно поднять сколько
        угодно копий сервиса, но если все запросы идут в одну, толку от копий
        не будет.
      </p>

      <div>
        <h3 className="font-semibold text-white">Клиентская и серверная</h3>
        <ul className="mt-2 list-disc space-y-1 pl-5">
          <li>
            <strong>Клиентская</strong> — клиент сам знает список инстансов и
            выбирает, к какому обратиться. Просто, но клиент должен следить за
            их живостью.
          </li>
          <li>
            <strong>Серверная</strong> — между клиентом и инстансами стоит
            отдельный узел (LB / nginx / L4-L7 балансировщик), который и
            принимает решение. Именно она разобрана в демо рядом.
          </li>
        </ul>
      </div>

      <div>
        <h3 className="font-semibold text-white">Стратегии выбора инстанса</h3>
        <ul className="mt-2 list-disc space-y-1 pl-5">
          <li>
            <strong>Round Robin</strong> — по кругу, инстанс за инстансом.
            Не учитывает ни вес, ни текущую нагрузку — самый простой вариант.
          </li>
          <li>
            <strong>Weighted Round Robin</strong> — тот же круг, но с весами:
            более мощный инстанс получает пропорционально больше запросов.
            Вес задаётся заранее (по ресурсам машины), а не по факту нагрузки.
          </li>
          <li>
            <strong>Least Connections</strong> — запрос уходит инстансу с
            наименьшим числом активных соединений. Единственная из трёх, что
            реагирует на фактическую загрузку в реальном времени; есть
            варианты по response time или bandwidth.
          </li>
          <li>
            <strong>Sticky Sessions</strong> — запросы одного и того же
            пользователя (по хэшу от его id) всегда идут на один и тот же
            инстанс — удобно, если сессия хранится в памяти инстанса, а не во
            внешнем сторе.
          </li>
        </ul>
      </div>

      <div>
        <h3 className="font-semibold text-white">L4 / L7 балансировка</h3>
        <p className="mt-2">
          L4-балансировщик работает на уровне TCP/UDP — просто раскидывает
          соединения, не заглядывая внутрь запроса, поэтому он быстрее.
          L7-балансировщик понимает HTTP (путь, заголовки, cookie) и может
          балансировать умнее — например, направлять разные пути к разным
          сервисам — но это дороже по ресурсам.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Где встречается на практике</h3>
        <p className="mt-2">
          nginx / HAProxy / Envoy перед пулом бэкендов, облачные Load Balancer
          (AWS ALB/NLB, GCP Load Balancing), geoDNS-балансировка между
          дата-центрами в разных регионах, а внутри Kubernetes — Service
          балансирует между подами.
        </p>
      </div>
    </div>
  );
}
