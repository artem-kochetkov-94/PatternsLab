export function Explanation() {
  return (
    <div className="space-y-4 text-slate-300">
      <p>
        <strong className="text-white">CRUD</strong> — четыре базовые
        операции с данными: создание, чтение, модификация, удаление. Разные
        стили API — это разные способы описать эти операции по сети.
      </p>

      <div>
        <h3 className="font-semibold text-white">REST</h3>
        <p className="mt-2">
          Глагол (GET/POST/PUT/DELETE) + существительное (URI ресурса) +
          заголовки + тело в JSON + код ответа. Простой, повсеместный,
          хорошо кэшируется на уровне HTTP. Главная слабость — негибкая
          форма ответа: чтобы собрать один экран из нескольких связанных
          ресурсов, часто нужно несколько запросов (демо рядом), а каждый
          отдаёт объект целиком, даже если нужно одно поле.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">GraphQL</h3>
        <p className="mt-2">
          Клиент сам описывает форму нужного ответа прямо в запросе — один
          запрос вместо нескольких, и сервер отдаёт ровно запрошенные поля.
          Кроме чтения (<code className="rounded bg-slate-800 px-1">query</code>
          ) есть запись (<code className="rounded bg-slate-800 px-1">mutation</code>
          ) и живые подписки на изменения (
          <code className="rounded bg-slate-800 px-1">subscription</code>).
          Плата за гибкость — HTTP-кэширование "из коробки" уже не работает
          так же просто, как с REST (все запросы обычно идут на один и тот
          же URL).
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">gRPC</h3>
        <p className="mt-2">
          RPC-класс: клиент вызывает функцию на удалённом сервисе так, будто
          она локальная. Контракт описывается в{" "}
          <code className="rounded bg-slate-800 px-1">.proto</code>-файле,
          данные сериализуются в бинарный Protocol Buffers (компактнее и
          быстрее JSON), поверх HTTP/2 — со стримингом в обе стороны. Внутри
          инфраструктуры (сервис-сервис) — частый выбор именно из-за
          скорости и строгой типизации; для браузерных клиентов не так
          удобен, как REST/GraphQL.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">SOAP</h3>
        <p className="mt-2">
          Протокол поверх XML-конвертов с собственной спецификацией ошибок
          и типов. Многословный и тяжеловесный по сравнению с современными
          альтернативами — на новых проектах почти не встречается, живёт
          в основном в legacy-интеграциях (банки, госсектор, старые
          корпоративные системы).
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Итого</h3>
        <p className="mt-2">
          SOAP — фактически мёртв на новых проектах. REST — рабочая лошадка
          для клиент-серверного взаимодействия. gRPC — внутренняя связь
          между сервисами, где важна скорость. GraphQL — там, где клиенту
          нужна гибкость в форме ответа (мобильные приложения с разными
          экранами, агрегация нескольких источников за один запрос).
        </p>
      </div>
    </div>
  );
}
