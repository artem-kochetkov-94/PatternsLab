export function Explanation() {
  return (
    <div className="space-y-4 text-slate-300">
      <p>
        <strong className="text-white">MapReduce</strong> — модель обработки огромных объёмов
        данных, которые не помещаются (и не должны обрабатываться) на одной машине. Задача
        разбивается на фазы, каждая из которых естественно параллелится.
      </p>

      <div>
        <h3 className="font-semibold text-white">Map</h3>
        <p className="mt-2">
          Входные данные режутся на независимые куски, и каждый кусок обрабатывается ОТДЕЛЬНО, без
          общения с другими Map-задачами. Результат — набор пар (ключ, значение). В word count
          ключ — слово, значение — единица (одно вхождение).
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Shuffle</h3>
        <p className="mt-2">
          Все пары со всех машин перемешиваются и группируются по ключу — одинаковые ключи
          собираются вместе, независимо от того, где именно они были посчитаны. Это единственная
          фаза, где данные реально перемещаются между узлами кластера.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Reduce</h3>
        <p className="mt-2">
          Каждая группа сворачивается в одно итоговое значение. В word count — суммой всех
          единиц по каждому слову, но операция может быть любой ассоциативной: максимум, среднее,
          конкатенация.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Почему это масштабируется</h3>
        <p className="mt-2">
          Map-задачи полностью независимы друг от друга — их можно раскидать по сколь угодно
          большому кластеру машин без координации между ними. Единственная точка синхронизации —
          Shuffle, и даже она делается инкрементально, по мере готовности Map-результатов.
          Hadoop, Spark и большинство систем batch-аналитики построены на этой идее.
        </p>
      </div>
    </div>
  );
}
