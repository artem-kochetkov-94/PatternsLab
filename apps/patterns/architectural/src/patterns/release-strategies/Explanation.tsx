export function Explanation() {
  return (
    <div className="space-y-4 text-slate-300">
      <p>
        Обновить прод так, чтобы не сломать его для живых пользователей, — отдельная задача.
        Три стратегии решают её по-разному: разница в том, что происходит с трафиком, пока
        обновление идёт.
      </p>

      <div>
        <h3 className="font-semibold text-white">Rolling Release</h3>
        <p className="mt-2">
          Инстансы обновляются по одному: снял с ротации → обновил → вернул → взял следующий.
          Простоя нет никогда, но какое-то время старая и новая версии обслуживают трафик
          ОДНОВРЕМЕННО — если между версиями несовместимый формат данных или API, это может
          привести к неожиданным багам.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Blue/Green Release</h3>
        <p className="mt-2">
          Держим два ПОЛНЫХ комплекта инфраструктуры одновременно — старый (Blue) и новый
          (Green). Переключение router — мгновенное и атомарное: либо весь трафик на Blue, либо
          весь на Green, смешанного состояния не бывает. Откат — тоже мгновенный, обратным
          переключением. Цена — двойной расход ресурсов на время раскатки.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Canary Release</h3>
        <p className="mt-2">
          Новая версия получает только небольшой процент реального трафика — «канарейка в
          шахте». Если метрики (ошибки, задержка) в порядке, процент постепенно увеличивают, пока
          не дойдёт до 100%. Если нет — откатывают, но пострадала лишь малая доля пользователей,
          а не вся аудитория сразу.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">Итого</h3>
        <p className="mt-2">
          Rolling — самый дешёвый по ресурсам, но с окном смешанных версий. Blue/Green —
          мгновенный и предсказуемый откат ценой двойной инфраструктуры. Canary — самый
          осторожный: минимизирует blast radius (сколько пользователей затронет проблема), но
          требует мониторинга и постепенного увеличения трафика.
        </p>
      </div>
    </div>
  );
}
