export function Explanation() {
  return (
    <div className="space-y-4 text-slate-300">
      <p>
        <strong className="text-white">Изоляция</strong> — одна из четырёх
        гарантий ACID: представление базы данных, которое транзакция видит в
        своём начале, должно меняться только ею самой до её завершения. На
        практике полная изоляция каждой транзакции от каждой — дорогая по
        производительности штука, поэтому большинство СУБД дают выбор:
        разменять часть строгости на скорость через{" "}
        <strong className="text-white">уровень изоляции</strong>.
      </p>

      <div>
        <h3 className="font-semibold text-white">Четыре аномалии</h3>
        <ul className="mt-2 list-disc space-y-1 pl-5">
          <li>
            <strong>Грязное чтение</strong> — читаем ещё не закоммиченные
            (и потенциально откатываемые) данные другой транзакции.
          </li>
          <li>
            <strong>Неповторяющееся чтение</strong> — дважды читаем одну и ту
            же строку в рамках одной транзакции и получаем разные значения,
            потому что между чтениями кто-то её изменил и закоммитил.
          </li>
          <li>
            <strong>Фантомное чтение</strong> — то же самое, но с набором
            строк: повторный агрегирующий запрос видит новую строку, которой
            не было при первом запросе.
          </li>
          <li>
            <strong>Потерянное обновление</strong> — две транзакции читают
            одно значение и обе пишут результат на его основе; та, что
            закоммитила позже, вслепую затирает работу первой.
          </li>
        </ul>
      </div>

      <div>
        <h3 className="font-semibold text-white">Четыре уровня изоляции</h3>
        <p className="mt-2">
          Каждый следующий уровень закрывает на одну аномалию больше
          предыдущего — это видно в демо рядом по переключателю:
        </p>
        <ul className="mt-2 list-disc space-y-1 pl-5">
          <li>
            <strong>READ UNCOMMITTED</strong> — не закрывает ничего, все
            четыре аномалии возможны.
          </li>
          <li>
            <strong>READ COMMITTED</strong> — закрывает грязное чтение
            (значение по умолчанию в PostgreSQL и большинстве СУБД).
          </li>
          <li>
            <strong>REPEATABLE READ</strong> — дополнительно закрывает
            неповторяющееся чтение и потерянное обновление.
          </li>
          <li>
            <strong>SERIALIZABLE</strong> — закрывает всё, включая фантомы;
            транзакции ведут себя так, будто выполняются строго по очереди.
          </li>
        </ul>
      </div>

      <div>
        <h3 className="font-semibold text-white">Как это реализовано</h3>
        <p className="mt-2">
          Два основных механизма: <strong>2PL</strong> (two-phase
          locking) — транзакция на фазе расширения набирает блокировки и не
          отпускает их до самого конца, на фазе сжатия отпускает все разом;
          и <strong>MVCC</strong> (multiversion concurrency control) —
          вместо блокировок каждая строка хранит несколько версий с
          идентификаторами создавшей и удалившей её транзакции, и каждая
          транзакция читает свой согласованный снимок без блокировки
          читателей писателями. PostgreSQL использует MVCC.
        </p>
      </div>

      <div>
        <h3 className="font-semibold text-white">За пределами ACID</h3>
        <p className="mt-2">
          NoSQL-базы часто вместо ACID предлагают{" "}
          <strong>BASE</strong> (Basically Available, Soft State, Eventual
          Consistency) — доступность и масштабируемость важнее строгой
          согласованности, а ответственность за неё частично перекладывается
          на разработчика приложения.
        </p>
      </div>
    </div>
  );
}
