export function Explanation() {
  return (
    <div className="space-y-4 text-slate-300">
      <p>
        <strong className="text-white">Command (Команда)</strong> —
        поведенческий паттерн, который превращает запрос (действие) в
        самостоятельный объект. Этот объект хранит всё необходимое для вызова:
        что сделать и над кем. Раз действие стало объектом, его можно передавать
        как параметр, складывать в очередь, логировать и — главное — отменять.
      </p>

      <div>
        <h3 className="font-semibold text-white">Когда применять</h3>
        <ul className="mt-2 list-disc space-y-1 pl-5">
          <li>
            Когда нужны отмена и повтор действий (Undo/Redo) — каждая команда
            знает, как откатить саму себя.
          </li>
          <li>
            Когда операции нужно ставить в очередь, выполнять отложенно,
            повторять или объединять в макросы/сценарии.
          </li>
          <li>
            Когда хочется отделить объект, инициирующий операцию (кнопку, пункт
            меню), от объекта, который знает, как её выполнить.
          </li>
        </ul>
      </div>

      <div>
        <h3 className="font-semibold text-white">Из чего состоит</h3>
        <ul className="mt-2 list-disc space-y-1 pl-5">
          <li>
            <strong>Command</strong> — интерфейс с методами{" "}
            <code className="rounded bg-slate-800 px-1">execute</code> и{" "}
            <code className="rounded bg-slate-800 px-1">undo</code>.
          </li>
          <li>
            <strong>ConcreteCommand</strong> — конкретная команда. Хранит ссылку
            на получателя и параметры, а для отмены — состояние, нужное для
            отката (стёртый текст, прежнее значение).
          </li>
          <li>
            <strong>Receiver (Получатель)</strong> — тот, кто реально выполняет
            работу (документ, умный дом). Не знает про команды.
          </li>
          <li>
            <strong>Invoker (Отправитель)</strong> — вызывает команды и хранит
            их историю/очередь. Работает только через интерфейс, не зная их
            деталей.
          </li>
          <li>
            <strong>Client</strong> — создаёт команды и связывает их с
            получателем.
          </li>
        </ul>
      </div>

      <div>
        <h3 className="font-semibold text-white">Две грани (см. вкладку «Демо»)</h3>
        <ul className="mt-2 list-disc space-y-1 pl-5">
          <li>
            <strong>Undo/Redo</strong> — Invoker держит две стопки: выполненных
            и отменённых команд. Отмена снимает команду с одной стопки и кладёт
            на другую. Так работают редакторы, графические инструменты, формы.
          </li>
          <li>
            <strong>Очередь и макрос</strong> — команды не выполняются сразу, а
            копятся, переставляются и запускаются пачкой. Несколько команд
            объединяются в составную (MacroCommand) и применяются как одна.
          </li>
        </ul>
      </div>

      <div>
        <h3 className="font-semibold text-white">Где встречается на практике</h3>
        <p className="mt-2">
          История изменений в Redux/redux-undo, action-объекты в Redux,{" "}
          <code className="rounded bg-slate-800 px-1">undo/redo</code> в
          редакторах, очереди задач и оптимистичные обновления, кнопки и пункты
          меню, привязанные к командам, а не к коду напрямую.
        </p>
      </div>
    </div>
  );
}
